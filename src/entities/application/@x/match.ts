/**
 * Application → Match cross-entity imports
 * FSD @x pattern for explicit entity relationships
 */

export {
  matchKeys,
  createMatchService,
  useMatches,
  useMatch,
  useCreateMatch,
  useUpdateMatch,
} from '@/entities/match';
