# Next Pages Router Placeholder

This directory exists to reserve Next.js `pages` at project root.
Do not add route implementations here.

FSD page composition lives in `src/pages/*` and is imported via `@/pages/*`.
Next App Router entry files are organized under route groups in `app/(home)`, `app/(auth)`, and `app/(main)`.
